```mermaid
sequenceDiagram
    participant Browser
    participant Server

    Browser->>Browser: User types a note into the text field
    Browser->>Browser: User clicks the Save button
    Browser->>Server: HTTP POST request to /new_note (contains new note data)
    Server-->>Browser: HTTP status 302 (Found) with Location: /notes (URL redirect)
    Browser->>Server: HTTP GET request to /notes
    Server-->>Browser: HTML document with updated notes
    Browser->>Browser: Renders the updated notes page (new note is visible)
