sequenceDiagram
    participant Browser
    participant Server

    Browser->>Browser: User types a note into the text field
    Browser->>Browser: User clicks the Save button
    Browser->>Server: HTTP POST request to /new_note_spa (contains new note data)
    Server-->>Browser: HTTP status 201 Created (new note saved)
    Browser->>Browser: JavaScript code adds the new note to the DOM
    Browser->>Browser: New note is displayed on the page (without full page reload)
